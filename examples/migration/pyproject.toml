[project]
name = "migration"
version = "0.1.0"
description = "Migration examples using speclib-python"
requires-python = ">=3.14"
dependencies = [
    "afspec @ git+https://github.com/agent-fox-dev/speclib-python.git",
]
