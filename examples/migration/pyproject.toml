[project]
name = "migration"
version = "0.1.0"
description = "Migration examples using speclib-python"
requires-python = ">=3.14"
dependencies = [
    "afspec",
]

[tool.uv.sources]
afspec = { path = "../../../speclib-python", editable = true }
