{
  "questions": [
    {
      "id": "q1",
      "text": "Can you provide a concise Intent statement for Phase 1? What is the single most important outcome this phase must deliver?",
      "context": "A clear Intent statement is required by the spec format and anchors all other decisions. Currently the intent is scattered across the background and requirements.",
      "options": [
        "Establish secure, multi-tenant foundations for af-hub by delivering user management, OAuth-based onboarding, API key lifecycle management, and repo-scoped RBAC.",
        "Enable the first external users to self-onboard to af-hub and authenticate AF instances against it via time-bound API keys.",
        "Other (please describe)"
      ],
      "required": true
    },
    {
      "id": "q2",
      "text": "What are the measurable success criteria / goals for Phase 1? For example: 'An admin can onboard a new user end-to-end in < 5 minutes', 'All API endpoints return 401 for expired tokens within 1 request cycle', etc.",
      "context": "Without measurable goals it is impossible to verify when Phase 1 is 'done'. These also drive QA and acceptance testing.",
      "options": [],
      "required": true
    },
    {
      "id": "q3",
      "text": "What is explicitly OUT of scope for Phase 1? For example: billing, advanced RBAC roles beyond 'editor', AF instance gateway/control-plane features, audit log querying UI, etc.",
      "context": "Non-Goals prevent scope creep and help engineers make trade-off decisions independently. The PRD currently has none.",
      "options": [
        "Gateway / control-plane features for AF instances",
        "UI / dashboard (CLI only in Phase 1)",
        "Additional RBAC roles beyond the initial 'editor' role",
        "Billing and subscription management",
        "Audit log query interface",
        "Other (please list)"
      ],
      "required": true
    },
    {
      "id": "q4",
      "text": "What is the tenant boundary in the multi-tenant model? Is a 'repo' the tenant, or is there a higher-level concept (e.g., 'organisation' or 'workspace') that owns multiple repos?",
      "context": "The requirement that 'all DB tables must be keyed with a repo ID' implies repo = tenant, but this conflicts with typical multi-tenant patterns where one organisation owns many repos. Clarifying this shapes the entire data model.",
      "options": [
        "Repo is the tenant \u2014 each repo is fully isolated.",
        "Organisation/team is the tenant \u2014 one org owns many repos; isolation is at the org level.",
        "Hybrid \u2014 repos are grouped under an org, but API keys and access can be scoped to either level."
      ],
      "required": true
    },
    {
      "id": "q5",
      "text": "How should identity federation across OAuth providers be handled? If a user signs up with GitHub and later logs in with Google using the same email, should they be treated as the same user?",
      "context": "This directly affects the user record schema and the onboarding flow. Mis-handling this can lead to duplicate accounts or unintended account merging.",
      "options": [
        "Email is the canonical identity \u2014 same email across providers = one account (auto-merge).",
        "Each provider identity is independent \u2014 no automatic merging; users must link accounts explicitly.",
        "First provider wins \u2014 subsequent logins with a different provider for the same email are rejected until the user links them."
      ],
      "required": true
    },
    {
      "id": "q6",
      "text": "What is the API key renewal/rotation model? When a key expires, what must happen?",
      "context": "The PRD states keys must be time-bound but does not define what 'renewal' means operationally. This affects both the API design and the CLI commands.",
      "options": [
        "User must explicitly create a new key; expired keys are permanently invalidated.",
        "Keys can be renewed (expiry extended) via an API call before or after expiration, keeping the same key value.",
        "A new key is issued on renewal; the old key is immediately invalidated.",
        "A grace period is allowed after expiry before the key stops working."
      ],
      "required": true
    },
    {
      "id": "q7",
      "text": "Which specific CLI commands are in scope for Phase 1?",
      "context": "The PRD mentions 'user self-signup and basic API key management' via CLI but does not enumerate the commands. This is needed to scope the CLI implementation work.",
      "options": [
        "signup, login, logout",
        "key create, key list, key revoke, key renew",
        "user show, user update",
        "All of the above",
        "Other / subset (please specify)"
      ],
      "required": false
    },
    {
      "id": "q8",
      "text": "What are the security requirements for API key storage and transport? For example: must keys be stored as hashes only (never in plaintext), must the API enforce TLS, what is the minimum key entropy?",
      "context": "These are non-functional requirements that directly influence the implementation and security review. They are entirely absent from the current PRD.",
      "options": [
        "Keys stored as bcrypt/argon2 hashes; plaintext shown only once at creation.",
        "Keys stored encrypted at rest; retrievable by admins.",
        "Follow OWASP API Security Top 10 as the baseline standard.",
        "Other (please describe)"
      ],
      "required": false
    }
  ],
  "answers": {
    "q1": "Enable the first external users to self-onboard to af-hub and authenticate AF instances against it via time-bound API keys.",
    "q2": "All API endpoints are implemented and tested in an end-to-end manner.",
    "q3": "billing, advanced RBAC roles beyond 'editor', AF instance gateway/control-plane features, audit log querying UI",
    "q4": "Repo is the tenant",
    "q5": "Email is the canonical identity \u2014 same email across providers = one account (auto-merge)",
    "q6": "A new key is issued on renewal; the old key is immediately invalidated",
    "q7": "signup, login, logout",
    "q8": "Keys stored as bcrypt/argon2 hashes; plaintext shown only once at creation."
  }
}
